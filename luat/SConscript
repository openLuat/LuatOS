Import('RTT_ROOT')
Import('rtconfig')
from building import *

cwd = GetCurrentDir()
src	= Glob('modules/*.c') + Glob('rtt/*.c')
CPPPATH = [cwd + "/inculde"]

group = DefineGroup('luat', src, depend = [''], CPPPATH = CPPPATH)

Return('group')
