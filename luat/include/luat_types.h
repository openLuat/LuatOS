#ifndef LUAT_TYPES_H
#define LUAT_TYPES_H

#define LUAT_RET int

enum {
    LUAT_ERR_OK   = (0) ,
    LUAT_ERR_FAIL = (-1),
    LUAT_TRUE     = (1) ,
    LUAT_FALSE    = (0) ,
    LUAT_NULL     = (0)
};

#endif

